{
  "name": "@surr/shared",
  "version": "0.0.0",
  "private": true,
  "main": "./lib/index.js",
  "types": "./lib/index.d.ts",
  "files": [
    "lib"
  ],
  "scripts": {
    "build": "tsc",
    "lint": "echo \"(no lint for shared)\"",
    "typecheck": "tsc --noEmit",
    "test": "echo \"(no tests for shared)\""
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
